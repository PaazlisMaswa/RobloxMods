-- loadstring(game:HttpGet('https://raw.githubusercontent.com/PaazlisMaswa/RobloxProject/refs/heads/main/Packages/Rayfield/ExampleRayfield'))()

local Rayfield=loadstring(game:HttpGet("http://raw.githubusercontent.com/PaazlisMaswa/RobloxProject/refs/heads/main/Packages/Rayfield/init.luau"))()

local Window=Rayfield:CreateWindow({
	Title="Rayfield",
	LoadingTitle="Panel",
	Theme="Default",
	Icon=0,
	LoadingSubtitle="by Sirius",
	ConfigurationSaving={
		Enabled=true,
		FolderName=nil, -- Create a custom folder for your hub/game
		FileName="SimpleHub"
	},
	Discord={
		Enabled=false,
		Invite="noinvitelink", -- The Discord invite code,do not include discord.gg/. E.g. discord.gg/ABCD would be ABCD
		RememberJoins=true -- Set this to false to make them join the discord every time they load it up
	},
	KeyInfo={
		Enabled=false, -- Set this to true to use our key system
		Title="Rayfield",
		Subtitle="Key System",
		Note="No method of obtaining the key is provided",
		FileName="Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
		SaveKey=true, -- The user's key will be saved,but if you change the key,they will be unable to use your script
		GrabKeyFromSite=false, -- If this is true,set Key below to the RAW site you would like Rayfield to get the key from
		Key={"Hello"}  -- List of keys that will be accepted by the system,can be RAW file links (pastebin,github etc) or simple strings ("hello","key22")
	}
})

local Tabs={
	Classic=Window:CreateTab("Mics","settings"),
	Lite=Window:CreateTab("Lite","chef-hat")
}

local Tab=Tabs.Classic

--! Section
local Section=Tab:CreateSection("Player")
--[[
Section:Set("Character")
]]

--! Paragraph
local Paragraph=Tab:CreateParagraph({
	Name="Example",
	["Content"]="by Rayfield"
})

--! Divider
local Divider=Tab:CreateDivider()

--! Label
local Label=Tab:CreateLabel("Hello World!")

--! Button
local Button=Tab:CreateButton({
	Name="Button",
	Callback=function()
		-- The function that takes place when the button is pressed
		warn("Button Pressed")
	end
})
--[[
Button:Set(<string>)
]]

--! Toggle
local Toggle=Tab:CreateToggle({
	Name="Camera Inverted",
	Value=false,
	Flag="Toggle1",-- A flag is the identifier for the configuration file,make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback=function(Value)
		-- The function that takes place when the toggle is pressed
		-- The variable (Value) is a boolean on whether the toggle is true or false
		warn("Toggle:",Value)
	end
})
--[[
Toggle:Replace(true) -- no callback
Toggle:Set(false) -- with callback
]]

--! Input
local Input=Tab:CreateInput({
	Name="Feedback",
	Value="Hallo",
	PlaceholderText="Type feedback here..",
	RemoveTextAfterFocusLost=false,
	Flag="Input1", -- A flag is the identifier for the configuration file,make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback=function(Text)
		-- The function that takes place when the input is changed
		-- The variable (Text) is a string for the value in the text box
		warn("Input:",Text)
	end
})
--[[
Input:Replace("Nice") -- no callback
Input:Set("Apple") -- with callback
]]

--! Dropdown
local Dropdown=Tab:CreateDropdown({
	Type="Dropdown",
	Name="Experience Language",
	MultipleOptions=false,
	Options={"English","Russian","Indonesian"},
	Option={"English"},
	Flag="Dropdown1", -- A flag is the identifier for the configuration file,make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback=function(Options)
		-- The function that takes place when the selected option is changed
		-- The variable (Options) is a table of strings for the current selected options
		warn("Dropdown:",table.concat(Options,","))
	end
})
--[[
Dropdown:Set({"Russian"})
]]


--! Slider
local Slider=Tab:CreateSlider({
	Name="Speed",
	Prefix="Speed",
	Range={16,32},
	Increment=1,
	Value=10,
	NoCap=false,
	Flag="Slider1",-- A flag is the identifier for the configuration file,make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback=function(Value)
		-- The function that takes place when the slider changes
		-- The variable (Value) is a number which correlates to the value the slider is currently at
		warn("Slider:",Value)
	end
})
--[[
Slider:Set(25)
SliderPro:Refresh()
]]

--! Keybind
local Keybind=Tab:CreateKeybind({
	Name="Open Keybind",
	Keybind="Q",
	HoldToInteract=false,
	Flag="Keybind1",-- A flag is the identifier for the configuration file,make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback=function(Value)
		-- The function that takes place when the keybind is pressed
		-- The variable (Value) is a boolean for whether the keybind is being held or not (HoldToInteract needs to be true)
		warn("Keybind:",Value)
	end,
})
--[[
Keybind:Set(Enum.KeyCode.E or "E")
]]

local UserInputService=game:GetService("UserInputService")
UserInputService.InputBegan:Connect(function(input,processed)
	if processed then return end
	if input.KeyCode.Name==Keybind.Keybind and UserInputService:IsKeyDown(input.KeyCode) then
		-- Put logic here
	end
end)



--! ColorPicker
local ColorPicker=Tab:CreateColorPicker({
	Name="Base Color",
	Color=Color3.fromRGB(255,255,255),
	Flag="ColorPicker",-- A flag is the identifier for the configuration file,make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback=function(Value)
		-- The function that takes place every time the color picker is moved/changed
		-- The variable (Value) is a Color3fromRGB value based on which color is selected
		warn("ColorPicker:",Value)
	end
})
--[[
ColorPicker:Set(Color3.fromRGB231,231,236))
]]


local NewTab=Tabs.Lite

--! PlayerPicker
local PlayerPicker=NewTab:CreatePlayerPicker()

--! Expand
local Expand=NewTab:CreateExpand({
	Name="Example",
	Active=false
})

local LabelExample=NewTab:CreateLabel("Hello World!")
Expand:Add(LabelExample)

--! Selector
local Selector=NewTab:CreateSelector({
	Name="Camera Mode",
	Options={"Classic","Follow","CameraToggle"},
	Value="Classic",
	NoCap=false, -- If true then unlimited chosen
	Flag="Selector1", -- A flag is the identifier for the configuration file,make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback=function(Value)
		-- The function that takes place when the choosen option is changed
		-- The variable (Value) is a string
		warn("Selector:",Value)
	end
})
--[[
Selector:Set("Follow")
]]

--! Select
local Select=Tab:CreateSelect({
	Type="Select",
	Name="Pick Target",
	Callback=function(Target)
		-- The function that takes place when the mouse is click object in world
		-- The variable (Target) is a Instance
		warn("Select:",Target.Name,Target.BrickColor.Name)
	end
})
--[[
Select:Set(<Instance>)
]]

local Players=game:GetService("Players")
local LocalPlayer=Players.LocalPlayer

repeat task.wait() until LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
local SavedWalkSpeed=LocalPlayer.Character:FindFirstChildOfClass("Humanoid").WalkSpeed

--! Slider Pro
local SliderPro=NewTab:CreateSlider({
	["Version"]=2,
	Name="Jump",
	Prefix="Jump",
	Percent=25,
	Value=SavedWalkSpeed,
	NoCap=false,
	Flag="SliderPro1",-- A flag is the identifier for the configuration file,make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback=function(Value)
		-- The function that takes place when the slider changes
		-- The variable (Value) is a number which correlates to the value the slider is currently at
		warn("SliderPro:",Value)
	end
})
--[[
SliderPro:Set(20)
SliderPro:Refresh()
]]

local function UpdateHumanoid(char)
	local humanoid=char:FindFirstChildOfClass("Humanoid")
	if humanoid then
		SavedWalkSpeed=humanoid.WalkSpeed
		SliderPro.Value=SavedWalkSpeed
		SliderPro:Refresh()
	end
end

local UpdateButton=NewTab:CreateButton({
	Type="Button",
	Name="Update",
	Callback=function()
		if LocalPlayer.Character then
			UpdateHumanoid(LocalPlayer.Character)
		end
	end
})

local Commands={
	{Type="Section",Name="Menu"},
	{Type="Button",Name="VIP",Callback=function() print("VIP Button Pressed") end}
}

for _,info in ipairs(Commands) do
	local NewContext=NewTab:AddContext(info)
	if info.Name=="VIP" then
		
	end
end

--! :Notify, :Destroy and :LoadConfiguration function should be placed below after CreateWindow is called.

Rayfield:LoadConfiguration()

Rayfield:Notify({Title="Rayfield Interface",Content="Welcome to Rayfield. These-are the brand new notification design for Rayfield,with custom sizing and Rayfield calculated wait times.",Duration=5,Image=4483362458})

-- Rayfield:Destroy() 
